# Corpus for snapshot-regression.py - one glob per line, relative to the repo root.
# Lines after '#' are ignored. Snapshots live in integration-tests/snapshots/ and are
# committed; adding a line here means running --update once and committing the result.
#
# 20260729 Claude: seeded with the synthetic fixtures plus the three real documents that
# were used as evidence when closing the participant-priority defect. The real samples are
# where this layer earns its keep - they are messier than anything we would write by hand.
# Add more freely; the cost is one --update and the disk space of the snapshot.

transforms/integration-tests/documents/*.xml

samples/cda/RR-R1_1/RR-CDA-001_R1_1.xml
samples/cda/RR-R1_1/RCKMSOutput_UT-MI_BothReportable_Test.xml-RR.xml
samples/cda/xspec-test-files/XSPEC_eICR-CDA-001_R3_1.xml

# 20260730 Claude: adopted after transform-vetting every document (21 docs, 0 transform
# errors). All of these directories are gitignored, so like the RCKMS document above they
# are LOCAL-ONLY coverage - CI prints "corpus pattern matched nothing" and skips them.
# Vetting found pre-existing dangling/empty references in five of the messier documents
# (see the defect-leads section of the codebase notes); the snapshots deliberately pin
# that current behavior - when the defects are fixed, exactly those documents will flag.
samples/cda/eICR-R3_1/*.xml
samples/cda/20260424-COIVD/*.xml
samples/cda/20260424-Flu/*.xml
samples/cda/20260424-Pertussis/*.xml
# eICR-R1_1 listed explicitly: eICRCDAR1.1.xml is a byte-identical duplicate of
# eICR-CDA-001_R1_1.xml and is deliberately left out.
samples/cda/eICR-R1_1/0dece6c3-b9ad-49a2-9614-cca37822a21a-Scenario 1_BocaRaton_Cerner.xml
samples/cda/eICR-R1_1/cda-fhir-bundle-eICRCDAR1.13.1.xml
samples/cda/eICR-R1_1/eICR-CDA-001_R1_1.xml
samples/cda/eICR-R1_1/eICR-CDA-002_R1_1.xml
samples/cda/eICR-R1_1/Test eICR (1).xml

# 20260902 Claude (per SG): the September scrubbed TN documents. Local-only like the rest -
# the directory is gitignored; snapshots + baseline lines are committed. TN2 is the document
# that drove the ECON-contact fallback/dedupe batch and the B18a DAR-system fix.
samples/cda/20260902-Scrubbed/*.xml
